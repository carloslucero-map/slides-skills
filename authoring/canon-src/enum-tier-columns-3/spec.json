{
  "id": "enum-tier-columns-3",
  "$comment": "Hand-written. Structure only.",
  "approvedBy": "Claude Opus 5",
  "approvedAt": "2026-09-11",
  "approvalNote": "Reviewed side by side against ref.png at full size, plus the composition and overflow gates. Structure, hierarchy, reading order and every departure from the source confirmed against the template's own header notes. This is a MACHINE approval: no human has read these against the bank. Treat it as a gate that has been honestly passed, not as sign-off by the deck owner.",
  "regions": [
    {
      "role": "eyebrow",
      "arity": 1,
      "required": true,
      "capacityRole": "label"
    },
    {
      "role": "headline",
      "arity": 1,
      "required": true,
      "capacityRole": "headline"
    },
    {
      "role": "tier",
      "repeat": true,
      "arity": [
        3,
        3
      ],
      "required": true,
      "children": [
        {
          "role": "indicator",
          "arity": 1,
          "required": true,
          "note": "n of 3 squares filled."
        },
        {
          "role": "levelName",
          "arity": 1,
          "required": true,
          "capacityRole": "cardTitle"
        },
        {
          "role": "sector",
          "arity": 1,
          "required": false,
          "capacityRole": "fineItem"
        },
        {
          "role": "problemTitle",
          "arity": 1,
          "required": true,
          "capacityRole": "cardTitle"
        },
        {
          "role": "verbatim",
          "arity": 1,
          "required": true,
          "capacityRole": "cardBody"
        }
      ]
    }
  ],
  "invariants": [
    "The indicator squares are what make these rungs rather than alternatives. columns-v1 gives three equal columns and says the opposite.",
    "Exactly three tiers, with 1, 2 and 3 squares filled. Four needs a different indicator and a different template.",
    "Each tier pairs a named level with a VERBATIM. The tier is the diagnosis, the quote is the evidence, and splitting them across slides loses both.",
    "Italic quotes with the operative clause in Medium — the one sanctioned bend in the weight rules, and only inside a quotation."
  ],
  "grounds": {
    "upper": "cream",
    "lower": "cream",
    "cards": "white"
  },
  "source": "bank:149",
  "measuredIn": "measure.json",
  "referenceRender": "ref.png"
}
